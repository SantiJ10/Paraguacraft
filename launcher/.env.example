# Copiá este archivo a `.env` (gitignored) y completá los valores.
# Nunca commitees `.env` al repositorio.

CURSEFORGE_API_KEY=
# Paraguabot: Groq tiene prioridad sobre OpenAI (API compatible OpenAI).
# Ubicaciones validas: launcher/.env, %APPDATA%/ParaguacraftLauncher/.env, o Ajustes > Paraguabot.
GROQ_API_KEY=
OPENAI_API_KEY=
